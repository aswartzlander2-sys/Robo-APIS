{
  "name": "robo-network-finder",
  "version": "1.3.0",
  "private": true,
  "description": "A standalone Robo-style network and IP detector with optional local IP data.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test server.test.js"
  }
}
