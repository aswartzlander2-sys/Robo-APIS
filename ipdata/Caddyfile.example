# Place this on the host serving apis.robo-universe.com.
# Start the Node application with HOST=127.0.0.1 and TRUST_PROXY=true.
# Caddy preserves /ipdata and passes the trusted client address in X-Forwarded-For.

apis.robo-universe.com {
    reverse_proxy 127.0.0.1:3000
}
